import { ColoreAllergeneDirective } from './colore-allergene.directive';

describe('ColoreAllergeneDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoreAllergeneDirective();
    expect(directive).toBeTruthy();
  });
});
